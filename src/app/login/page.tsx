import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Login',
  description: 'Login to your account to begin adding tasks!',
};

const LoginPage = () => {
  return <div>Login</div>;
};

export default LoginPage;
